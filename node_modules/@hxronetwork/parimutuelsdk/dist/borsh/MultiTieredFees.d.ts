export declare class MultiTieredFees {
    enum: number;
    version: number;
    tiers: number;
    stakedAmount: Array<number>;
    settlementFeeBps: Array<number>;
    _padding1: number;
    constructor(args: {
        version: number;
        tiers: number;
        stakedAmount: Array<number>;
        settlementFeeBps: Array<number>;
    });
}
